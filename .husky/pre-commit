#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Load nvm manually
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Debug info (optional)
echo "Node path: $(which node)"
node -v

# Run tests
yarn test
