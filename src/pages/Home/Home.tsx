import TodoCard from '@/components/todo/TodoCard';
import { Button } from '@/components/ui/button';

const Home = () => {
  return (
    <div>
      <h1>This is Home</h1>
      <Button>hello</Button>
      <TodoCard />
    </div>
  );
};

export default Home;
